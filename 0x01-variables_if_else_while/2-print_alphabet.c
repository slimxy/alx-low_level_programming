#include <stdio.h>

/**
* main - prints alphabet in lowercase
* Return: Always 0 (Success)
*/
iint main(void)
{
	char alpha

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpha);
	}
		piutchar('\n');
	return (0);
}

